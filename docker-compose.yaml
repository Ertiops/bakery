version: '3.8'

services:
  bakery:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bakery_app
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped