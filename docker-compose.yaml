version: '3.8'

services:
  challenge_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bakery_bot_app
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped